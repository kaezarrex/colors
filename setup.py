from setuptools import setup, find_packages

setup(
    name='tables.io',
    version='1',
    description='The tables.io web server and TableFile reader',
    author='Tim Johnson',
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)